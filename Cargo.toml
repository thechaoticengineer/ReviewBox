[package]
name = "reviewbox"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "A terminal review inbox for GitHub commits"
license = "MIT"

[dependencies]
crossterm = "=0.29.0"
ratatui = { version = "=0.30.2", default-features = false, features = ["crossterm"] }

